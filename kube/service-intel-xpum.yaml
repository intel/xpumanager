apiVersion: v1
kind: Service
metadata:
  labels:
    app: intel-xpumanager
  name: intel-xpumanager
  namespace: default
spec:
  ports:
  - name: metrics
    port: 30000
    protocol: TCP
  selector:
    app: intel-xpumanager
  sessionAffinity: None
