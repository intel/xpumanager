apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "xpumd.fullname" . }}-config
  labels:
    {{- include "xpumd.labels" . | nindent 4 }}
data:
  config.yaml: |-
{{ .Values.config | toYaml | indent 4 }}
